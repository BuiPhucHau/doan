import { Routes } from "@angular/router";
import { PostLocationComponent } from "./post-location.component";

export const POSTLOCATION_ROUTES: Routes = [
    {
        path: '',
        component: PostLocationComponent
    }
];